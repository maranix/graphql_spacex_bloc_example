name: spacex_graphql
description: A starting point for Dart libraries or applications.
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.16.2 <3.0.0'


dependencies:
  graphql: ^5.1.1
  json_annotation: ^4.5.0

dev_dependencies:
  build_runner: ^2.1.10
  graphql_codegen: ^0.7.2
  json_serializable: ^6.2.0
  mocktail: ^0.3.0 # For testing our library
  lints: ^1.0.1
  test: ^1.21.1
